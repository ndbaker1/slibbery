name: CI Pipeline
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Build NVML Example
        run: |
          cd examples/nvml
          ./setup.sh
          ./test.sh
